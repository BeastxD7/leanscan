# Copy to .env.local for local dev. Set these in your hosting provider
# (Vercel, etc.) for production.

# Public URL of the deployed site (no trailing slash). Used for canonical URLs,
# OG image absolute paths, sitemap.xml, robots.txt host, JSON-LD.
NEXT_PUBLIC_SITE_URL=https://leanscan.app
